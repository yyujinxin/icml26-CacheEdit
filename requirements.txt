# Core dependencies
torch>=2.0.0
diffusers>=0.27.0
transformers>=4.38.0
accelerate>=0.27.0

# Image processing
pillow>=10.0.0

# Data processing
numpy>=1.24.0
pandas>=2.0.0

# Utilities
tqdm>=4.65.0
requests>=2.31.0

# Optional dependencies (install separately if needed)
# flash-attn>=2.5.0  # For flash attention optimization
# fused-kernels      # For fused kernel operations
lpips>=0.1.4         # For PSNR/SSIM/LPIPS quality evaluation scripts
